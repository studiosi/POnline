<?php

	// Error Handling
	/*
	use Symfony\Component\HttpFoundation\Response;

	global $app;

	$app->error(function (\Exception $e, $code) use ($app) {

		if ($app['debug']) return; // If we are in debug mode we show everything

		return new Response($app['twig']->render('error.twig'));

	});*/
